'use client';

/**
 * TemplateSelector - DEPRECATED
 * This component is no longer used.
 * The new template system uses generateLuxuryPropertyElement from lib/template-html-renderer.ts
 * which renders templates as HTML/CSS instead of Canvas elements.
 * 
 * The new template page at /app/dashboard/template/page.tsx handles template customization directly.
 */

// This file is kept for backwards compatibility but is not actively used.
// Please use the template page at /app/dashboard/template/page.tsx instead.

export default function TemplateSelector() {
  return null;
}
